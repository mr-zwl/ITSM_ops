apiVersion: v1
kind: Service
metadata:
  name: itsm-backend
spec:
  selector:
    app.kubernetes.io/name: itsm-backend
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http

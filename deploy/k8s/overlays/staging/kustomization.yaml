apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: staging-
patches:
  - target:
      kind: Deployment
      name: itsm-backend
    patch: |
      - op: replace
        path: /spec/replicas
        value: 2
  - target:
      kind: ConfigMap
      name: itsm-config
    patch: |
      - op: replace
        path: /data/APP_ENV
        value: staging
